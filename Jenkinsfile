pipeline {
   agent any
   stages {
      stage('Build') {
         steps {
            echo 'BUILDING PROJECT!!'
         }
      }
      stage('Test') {
         steps {
            echo 'Testing in process'
         }
      }
      stage('Deploy') {
         steps {
            echo 'Deployment in process'
         }   
      }
   }
}

